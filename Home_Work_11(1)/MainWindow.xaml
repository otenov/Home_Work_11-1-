<Window x:Class="Home_Work_11_1_.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Home_Work_11_1_"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75*" ></ColumnDefinition>
            <ColumnDefinition Width="25*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition > </RowDefinition>
                <RowDefinition Height="50"> </RowDefinition>
            </Grid.RowDefinitions>
        <Button x:Name="Button1" 
                Content="Просмотр" 
                FontSize="20" 
                Grid.Row="1"
                Click="Button1_Click">
            
        </Button>
        </Grid>

        <ListView x:Name="ListClients"  Grid.Column="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding Surname}" Width="100" />
                    <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding FName}" Width="100"/>
                    <GridViewColumn Header="Отчество" DisplayMemberBinding="{Binding LName}" Width="100" />
                    <GridViewColumn Header="Телефон" DisplayMemberBinding="{Binding TelephoneNumber}" Width="100" />
                    <GridViewColumn Header="Паспорт" DisplayMemberBinding="{Binding pasport}" Width="200" />
                </GridView>
            </ListView.View>
        </ListView>
        
    </Grid>
</Window>
